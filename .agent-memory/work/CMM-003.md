# CMM-003 Publish the Unity package with semantic versions through a scoped registry

**Ready status:** Ready

**Team:** Atlas (advisor) · Forge (lead) · Sentinel (reviewer)

## Goal

- Make Unity Package Manager resolve this package by semantic version instead of a Git URL.
- Prepare a safe release, upgrade, and rollback workflow without breaking current consumers.

## Constraints

- Repository content must remain English-only and use the canonical AI Unity MCP Server name.
- Preserve compatibility identifiers unless a migration path is explicit and tested.
- Do not publish externally or create a release without valid credentials and an evidence-backed target registry.

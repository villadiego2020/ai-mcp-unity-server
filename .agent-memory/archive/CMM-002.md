# CMM-002 Integrate the MCP bridge with Unity CLI and com.unity.pipeline

**Status:** Complete

**Team:** Atlas (advisor) · Pulse (advisor) · Forge (lead) · Sentinel (reviewer)

## Result

- Pinned the official experimental `com.unity.pipeline` dependency to `0.6.0-exp.1`.
- Added `ai_mcp_list_commands` and `ai_mcp_dispatch` as native Unity Pipeline commands.
- Kept the existing Node/TCP transport and all 73 manifest commands unchanged.
- Routed both native commands through `MCPHandlers`, preserving the existing rate limit and write gate.
- Added structured failure translation so dispatcher failures surface as Pipeline command failures.

## Verification

- Node contract suite passed: 17/17.
- Production and Editor tests compiled against Unity 6000.0.75f1, actual Pipeline 0.6.0-exp.1 APIs, and Unity Newtonsoft.
- Manifest parity passed with 73 unique routes.
- English-only, branding, syntax, and diff checks passed.
- Live CLI discovery was blocked before package loading by the local Unity Package Manager error: `The "path" argument must be of type string. Received undefined`.

## Watch

- Re-run Unity Test Runner and native CLI discovery after the local UPM host is healthy.
- Native CLI timeouts cannot cancel Unity work that has already started; a timed-out mutation can have an unknown outcome.
- The pinned Pipeline package is experimental and remains a mandatory dependency for this integration.

## REFUTED

- The official Unity CLI/Pipeline integration is not imaginary or unavailable: Unity CLI 1.0.0-beta.5 and `com.unity.pipeline` 0.6.0-exp.1 were verified from the installed CLI, registry metadata, and package source.
- Replacing the existing transport was rejected; the official path is side-by-side and delegates to the same dispatcher.


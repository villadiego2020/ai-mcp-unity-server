# CMM-010 Native Unity MCP integration

**Status:** Done

## CMM-010 Result

- Atlas planned, Forge implemented, and Sentinel independently verified version 2.1.0.
- Optional Assistant adapter exposes all 73 manifest tools with matching input schemas through public Native MCP registration APIs.
- Connections provides project/instance identity, native status and enabled counts, client setup shortcuts, and explicit read-only starts.
- Native, Node/TCP, Pipeline and Editor calls share dispatcher authorization and source-labelled Activity. Built-in Unity tools retain separate permissions and logs.
- Write authorization now uses per-Editor SessionState instead of global EditorPrefs; domain reload preserves it, and a fresh Editor defaults OFF.
- Native registration recovers after registry refresh, preserves explicit disables, and never removes tools replaced by another owner.
- Pipeline discovery includes the shared schemas; Node remains supported without Assistant.
- Work commit: `68c0481`.

## CMM-010 Verification

- Unity 6000.5.10f1 without Assistant: real package import and 74/74 EditMode tests passed.
- Unity 6000.5.10f1 with Assistant 2.18.0-pre.2: final 81/81 EditMode tests passed.
- Node suite: 26/26 tests passed. Release verifier passed for v2.1.0.
- Official relay: 7/7 checks passed, including all 73 schemas compared with real Node discovery, ping, UI Toolkit inspection, invalid-input and denied-write errors, and verified PNG file delivery.
- Native worked with custom TCP stopped and writes OFF. Persisted Activity retained the Native source and denied-write error state.
- Core and Editor tests statically compiled against Unity 6000.0.75f1; this is not a runtime test of that version.
- All isolated Unity test processes exited cleanly. Manager2026 and global write preferences were not modified.
- Evidence directory: `C:/Users/villa/AppData/Local/Temp/ai-mcp-unity-cmm010-ac92f0fadf6e4d07935bafbbc5d0dc90`.

## CMM-010 Compatibility and rejected assumptions

- Assistant 2.18.0-pre.2 documentation deprecates Native MCP in favor of Unity CLI. Native remains an optional compatibility path guarded to Assistant versions from 2.18.0-pre.2 through 2.18.x.
- REFUTED: registering only CliCommand adapters also registers Native tools. The two systems have separate registries.
- REFUTED: returning native content image blocks provides inline screenshots. The tested Unity relay drops them; the original failing wire evidence is retained as `relay-rich-content-original-failure.json`.
- Native now returns verified local PNG paths and imageDelivery instructions without discarded base64 overhead. Node/TCP remains the inline-image option; remote clients need access to the local file.
- Tests do not claim that all 73 tools were executed end-to-end or that the Connections window received visual interaction QA.
- Pre-existing Memory lists CMM-003 open while the tracker reports Done; that unrelated drift was preserved.

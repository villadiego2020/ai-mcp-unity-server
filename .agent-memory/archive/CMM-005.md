# CMM-005 Rename folder and project to ai-mcp-unity-server

**Status:** Complete

**Team:** Atlas (advisor) · Forge (lead) · Sentinel (reviewer)

## Goal

- Rename the repository folder and project identity to `ai-mcp-unity-server`.
- Keep `AI Unity MCP Server` as the human-facing display name.

## Constraints

- Preserve compatibility identifiers where changing them would break installed Unity projects without a supported migration path.
- Repository source, tests, and documentation must contain no Thai text or forbidden legacy branding.
- Rename the Windows folder only after implementation and verification are complete.

## Decision

- Use `ai-mcp-unity-server` for the repository folder, Node bridge package, Git URL, tracker project, and external clone examples.
- Keep `AI Unity MCP Server` as the human-facing display name.
- Keep `com.mcpbridge`, `MCPBridge.*`, and `AIUnityMCPServer` as compatibility identifiers.
- Unity requires reverse-domain package IDs and does not support renaming an existing package through a version update.

## Implementation

- Moved the repository contents and `.git` metadata to `C:\\Work\\git\\ai-mcp-unity-server`.
- Updated the Node bridge package and lockfile name to `ai-mcp-unity-server`.
- Updated README, changelog, path fixtures, and naming regression tests.
- Migrated Baowan and BOBOFarmVille local package paths to the new folder.
- Migrated Manager2026 to `https://github.com/villadiego2020/ai-mcp-unity-server.git`.
- Updated the global Codex `unity` MCP registration to launch the bridge from the new path.
- Updated BOBOPM project CMM to the new name, disk path, and Unity stack.

## Verification

- Node tests passed: 14/14.
- JavaScript syntax, JSON parsing, English-only, legacy-brand, and project-identity scans passed.
- C# production and changed Editor test fixture compiled with zero errors.
- All six consumer manifest/lock files parse and retain dependency key `com.mcpbridge`.

## Watch

- The active Codex task keeps the now-empty `C:\\Work\\git\\com.mcpbridge` directory handle open. Its contents are fully moved; delete the empty shell after this task closes.
- Unity Test Framework execution was not run; static compilation covered the changed C# fixture.

## Delivery

- Project changes are intentionally uncommitted and unpushed in this task.

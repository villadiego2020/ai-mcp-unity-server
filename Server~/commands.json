{
  "_note": "SINGLE SOURCE for Unity MCP commands. The Node bridge (index.js) and Unity dispatcher (MCPHandlers.cs) both read this file. Add or edit manifest tools here only. Instance-management tools are local logic defined in index.js.",
  "commands": [
    { "tool": "unity_ping", "command": "ping", "path": "/ping", "description": "Check Unity Editor connection (active/selected instance)", "params": {} },
    { "tool": "unity_compile", "command": "compile", "path": "/compile", "description": "Trigger Unity AssetDatabase.Refresh + RequestScriptCompilation — use after writing new scripts so Unity detects and compiles them without manual interaction", "params": {} },
    { "tool": "unity_compile_status", "command": "compile_status", "path": "/compile-status", "description": "Check if Unity is still compiling scripts. Returns {isCompiling, status}. Poll this after unity_compile until isCompiling=false before calling unity_add_component or any type-dependent command.", "params": {} },
    { "tool": "unity_server_stop", "command": "server_stop", "path": "/server/stop", "description": "Stop AI Unity MCP Server (TcpListener) from the AI side — used by /unity-mcp-close to fully close MCP. The server stops after this response flushes, so a subsequent unity_ping failing means stop succeeded.", "params": {} },
    { "tool": "unity_optimize_ui", "command": "optimize_ui", "path": "/ui/optimize", "description": "Optimize UI in the active scene: disable raycastTarget on non-interactive Image/Text, disable pixelPerfect on Canvas, warn about heavy LayoutGroups. Requires Write ON.", "params": {} },
    { "tool": "unity_scene_list", "command": "scene_list", "path": "/scene/list", "description": "List all scenes in the project", "params": {} },
    { "tool": "unity_scene_hierarchy", "command": "scene_hierarchy", "path": "/scene/hierarchy", "description": "Get current scene hierarchy", "params": {} },
    { "tool": "unity_create_gameobject", "command": "create_gameobject", "path": "/gameobject/create", "description": "Create a GameObject in the active scene", "params": {
      "name": { "type": "string", "desc": "GameObject name" },
      "primitive": { "type": "enum", "values": ["cube","sphere","cylinder","plane","capsule","quad"], "opt": true, "desc": "Primitive type (optional)" },
      "parent": { "type": "string", "opt": true, "desc": "Parent GameObject name (optional)" },
      "x": { "type": "number", "opt": true, "default": 0, "desc": "Position X" },
      "y": { "type": "number", "opt": true, "default": 0, "desc": "Position Y" },
      "z": { "type": "number", "opt": true, "default": 0, "desc": "Position Z" }
    } },
    { "tool": "unity_delete_gameobject", "command": "delete_gameobject", "path": "/gameobject/delete", "description": "Delete a GameObject by name from the active scene", "params": {
      "name": { "type": "string", "desc": "GameObject name to delete" }
    } },
    { "tool": "unity_create_prefab", "command": "create_prefab", "path": "/prefab/create", "description": "Create and save a Prefab asset to disk", "params": {
      "name": { "type": "string", "desc": "Prefab name" },
      "folder": { "type": "string", "opt": true, "default": "Assets/Prefabs", "desc": "Destination folder path" }
    } },
    { "tool": "unity_create_ui", "command": "create_ui", "path": "/ui/create", "description": "Create a UI element in the active scene (auto-creates Canvas if needed)", "params": {
      "name": { "type": "string", "desc": "Element name" },
      "type": { "type": "enum", "values": ["button","text","image","panel"], "desc": "UI element type" },
      "text": { "type": "string", "opt": true, "desc": "Text content (for text/button)" },
      "x": { "type": "number", "opt": true, "default": 0, "desc": "Anchored position X" },
      "y": { "type": "number", "opt": true, "default": 0, "desc": "Anchored position Y" },
      "width": { "type": "number", "opt": true, "default": 160, "desc": "Width" },
      "height": { "type": "number", "opt": true, "default": 40, "desc": "Height" }
    } },
    { "tool": "unity_create_terrain", "command": "create_terrain", "path": "/terrain/create", "description": "Create a new Terrain in the scene, optionally generating hills with Perlin noise. For a flat floor use unity_create_gameobject with primitive 'plane'.", "params": {
      "name": { "type": "string", "opt": true, "default": "Terrain", "desc": "Terrain name" },
      "width": { "type": "number", "opt": true, "default": 500, "desc": "Width" },
      "length": { "type": "number", "opt": true, "default": 500, "desc": "Length" },
      "height": { "type": "number", "opt": true, "default": 100, "desc": "Max terrain height" },
      "generate": { "type": "boolean", "opt": true, "default": false, "desc": "Generate Perlin-noise hills" },
      "scale": { "type": "number", "opt": true, "default": 0.01, "desc": "Perlin noise scale" },
      "amplitude": { "type": "number", "opt": true, "default": 0.3, "desc": "Height multiplier 0..1" }
    } },
    { "tool": "unity_terrain_set_heights", "command": "set_terrain_heights", "path": "/terrain/set-heights", "description": "Apply heightmap to Terrain in the active scene (use scale for Perlin noise, or provide raw heights array)", "params": {
      "scale": { "type": "number", "opt": true, "default": 0.02, "desc": "Perlin noise scale (used when heights not provided)" },
      "heights": { "type": "number[]", "opt": true, "desc": "Flat float array (sqrt(len) x sqrt(len)) of 0..1 height values" }
    } },
    { "tool": "unity_find_asset", "command": "find_asset", "path": "/asset/find", "description": "Search for assets in the project by name and/or type", "params": {
      "name": { "type": "string", "desc": "Asset name or search term" },
      "type": { "type": "string", "opt": true, "desc": "Asset type e.g. Prefab, Texture2D, Material, Scene" }
    } },
    { "tool": "unity_place_prefab", "command": "place_prefab", "path": "/prefab/place", "description": "Instantiate an existing prefab asset into the active scene by name", "params": {
      "name": { "type": "string", "desc": "Prefab name to search for (e.g. P_HumanTrooperSword)" },
      "x": { "type": "number", "opt": true, "default": 0, "desc": "World position X" },
      "y": { "type": "number", "opt": true, "default": 0, "desc": "World position Y" },
      "z": { "type": "number", "opt": true, "default": 0, "desc": "World position Z" }
    } },
    { "tool": "unity_create_material", "command": "create_material", "path": "/material/create", "description": "Create a Material asset (URP Lit by default) with optional hex color", "params": {
      "name": { "type": "string", "desc": "Material name" },
      "shader": { "type": "string", "opt": true, "desc": "Shader name, default 'Universal Render Pipeline/Lit'" },
      "color": { "type": "string", "opt": true, "desc": "Hex color e.g. #FF0000" },
      "folder": { "type": "string", "opt": true, "default": "Assets/Materials", "desc": "Destination folder" }
    } },
    { "tool": "unity_create_sprite_atlas", "command": "create_sprite_atlas", "path": "/atlas/create", "description": "Pack all sprites in a folder into a Sprite Atlas to reduce draw calls", "params": {
      "name": { "type": "string", "desc": "Atlas name" },
      "folder": { "type": "string", "desc": "Folder containing sprites to pack" }
    } },
    { "tool": "unity_audit_textures", "command": "audit_textures", "path": "/audit/textures", "description": "Report textures that should be optimized (oversized, uncompressed, read/write enabled)", "params": {} },
    { "tool": "unity_audit_unused", "command": "audit_unused", "path": "/audit/unused", "description": "Report assets that may be unused (REPORT ONLY — does not delete; Addressables/Resources not detected)", "params": {} },
    { "tool": "unity_audit_empty_folders", "command": "audit_empty_folders", "path": "/audit/empty-folders", "description": "Report empty folders under Assets/", "params": {} },
    { "tool": "unity_count_components", "command": "count_components", "path": "/scene/count", "description": "Count objects with a component type, split into active vs inactive (inactive = pooled / SetActive false). Returns total, active, inactive, activeAndEnabled, and name lists.", "params": {
      "type": { "type": "string", "desc": "Component type name, e.g. 'Fusion.NetworkObject' or 'Rigidbody'" }
    } },
    { "tool": "unity_read_console", "command": "read_console", "path": "/console/read", "description": "Read recent Unity Console entries (errors, warnings, logs) — use to debug compile/runtime errors", "params": {
      "max": { "type": "number", "opt": true, "default": 30, "desc": "Max recent entries" }
    } },
    { "tool": "unity_inspect_object", "command": "inspect_object", "path": "/object/inspect", "description": "Read all components and their property values of a GameObject. Set deep=true for full reflection (private fields + all public properties, not just serialized ones).", "params": {
      "name": { "type": "string", "desc": "GameObject name" },
      "deep": { "type": "boolean", "opt": true, "default": false, "desc": "Use deep reflection (private fields + all public properties)" }
    } },
    { "tool": "unity_add_component", "command": "add_component", "path": "/object/add-component", "description": "Add a component/script to a GameObject", "params": {
      "name": { "type": "string", "desc": "GameObject name" },
      "component": { "type": "string", "desc": "Component type name, e.g. Rigidbody" }
    } },
    { "tool": "unity_set_property", "command": "set_property", "path": "/object/set-property", "description": "Set a serialized property value on a component (e.g. maxHp=100). Leave component empty for Transform.", "params": {
      "name": { "type": "string", "desc": "GameObject name" },
      "component": { "type": "string", "opt": true, "desc": "Component type name; empty = Transform" },
      "property": { "type": "string", "desc": "Property name or display name" },
      "value": { "type": "string", "desc": "Value as string (number/bool/text/enum, or 'x,y,z' for Vector3, '#RRGGBB' for Color)" }
    } },
    { "tool": "unity_set_transform", "command": "set_transform", "path": "/object/set-transform", "description": "Set position/rotation/scale of a GameObject. 'set' lists which to apply: pos, rot, scale", "params": {
      "name": { "type": "string", "desc": "GameObject name" },
      "set": { "type": "string", "desc": "Which to apply, e.g. 'pos,scale'" },
      "px": { "type": "number", "opt": true, "default": 0 }, "py": { "type": "number", "opt": true, "default": 0 }, "pz": { "type": "number", "opt": true, "default": 0 },
      "rx": { "type": "number", "opt": true, "default": 0 }, "ry": { "type": "number", "opt": true, "default": 0 }, "rz": { "type": "number", "opt": true, "default": 0 },
      "sx": { "type": "number", "opt": true, "default": 1 }, "sy": { "type": "number", "opt": true, "default": 1 }, "sz": { "type": "number", "opt": true, "default": 1 }
    } },
    { "tool": "unity_get_selection", "command": "get_selection", "path": "/selection/get", "description": "Get currently selected GameObjects in the editor", "params": {} },
    { "tool": "unity_set_selection", "command": "set_selection", "path": "/selection/set", "description": "Select a GameObject in the editor by name (and ping it)", "params": {
      "name": { "type": "string", "desc": "GameObject name" }
    } },
    { "tool": "unity_open_scene", "command": "open_scene", "path": "/scene/open", "description": "Open a scene by asset path (prompts to save current changes)", "params": {
      "path": { "type": "string", "desc": "e.g. Assets/Scenes/Main.unity" }
    } },
    { "tool": "unity_save_scene", "command": "save_scene", "path": "/scene/save", "description": "Save all open scenes", "params": {} },
    { "tool": "unity_read_logfile", "command": "read_logfile", "path": "/console/logfile", "description": "Read the tail of the Unity Editor.log (full stack traces + Debug.Log history) — better than console for diagnosing hangs", "params": {
      "max": { "type": "number", "opt": true, "default": 120, "desc": "Max recent lines" }
    } },
    { "tool": "unity_capture_state", "command": "capture_state", "path": "/diagnose/state", "description": "Capture a runtime snapshot (isPlaying, paused, timeScale, frameCount, fps, network, spikes) — use to diagnose why the game is stuck/frozen. Call twice to check if frameCount advances.", "params": {} },
    { "tool": "unity_perf_audit", "command": "perf_audit", "path": "/perf/audit", "description": "Full performance audit: scene census (renderers, skinned meshes, particles, realtime lights, animators, mesh colliders), heavy object groups (e.g. trees x500, creeps x40), network ping/bandwidth/loss, heuristic warnings, and captured FPS spikes with their cause. Use to diagnose lag/FPS drops and recommend optimizations.", "params": {} },
    { "tool": "unity_clear_console", "command": "clear_console", "path": "/console/clear", "description": "Clear the Unity console (use before reproducing a bug to isolate new errors)", "params": {} },
    { "tool": "unity_play_control", "command": "play_control", "path": "/play/control", "description": "Control Play Mode to reproduce/observe bugs: enter, exit, pause, resume, step, or timescale (slow-mo). Use timescale with scale=0.1-0.25 to watch fast events (hit/knockback/spawn) in slow motion while watches keep sampling; resume normal speed with scale=1 (exit also restores it).", "params": {
      "action": { "type": "enum", "values": ["enter","exit","pause","resume","step","timescale"], "desc": "Play mode action" },
      "scale": { "type": "number", "opt": true, "default": 0.2, "desc": "For action=timescale: Time.timeScale (0.01-100; <1 = slow-mo, 1 = normal)" }
    } },
    { "tool": "unity_read_script", "command": "read_script", "path": "/script/read", "description": "Read a project script's source with line numbers (optionally just one method) — use to cite exact problem lines", "params": {
      "name": { "type": "string", "desc": "Script name, e.g. FogofWars" },
      "method": { "type": "string", "opt": true, "desc": "Only return this method's body, e.g. Update" }
    } },
    { "tool": "unity_deep_analysis", "command": "diagnose_deep", "path": "/diagnose/deep", "description": "Full profiler deep analysis: top GC allocators, CPU self-time per method, suspicious patterns (FindObjectOfType/Instantiate/LINQ in hot path), and actual source code of offenders with line numbers. Use after unity_perf_audit finds issues or when diagnosing stutters/lag spikes.", "params": {
      "topN": { "type": "number", "opt": true, "default": 8, "desc": "Top N offenders to show per category" }
    } },
    { "tool": "unity_memory_snapshot", "command": "memory_snapshot", "path": "/diagnose/memory", "description": "Memory usage snapshot: Mono heap/used, Unity native alloc/reserved, graphics driver memory, and GC collection counts per generation. Use to detect memory leaks, excess allocations, or high graphics memory usage.", "params": {} },
    { "tool": "unity_fusion_stats", "command": "fusion_stats", "path": "/diagnose/fusion", "description": "Detailed Photon Fusion 2 network stats: current tick, tick rate, simulation time, per-player RTT, in/out bandwidth (KB/s), packet loss, resend rate, resimulation count, snapshot delta size. Requires Play mode with a NetworkRunner in scene.", "params": {} },
    { "tool": "unity_perf_worst", "command": "perf_worst", "path": "/perf/worst", "description": "Report the single worst FPS spike captured by the Auto Spike Monitor: frame time, GC culprit, CPU culprit, and actual source code of the offending method. Use after a stutter occurs in Play mode.", "params": {} },
    { "tool": "unity_refactor_audit", "command": "refactor_audit", "path": "/code/refactor-audit", "description": "Scan all project .cs files and report refactoring opportunities: large classes, long/complex methods, high coupling (fan-in/fan-out), deep inheritance, public fields, magic numbers, and TODO debt. Returns top N offenders with severity scores and actionable issues.", "params": {
      "topN": { "type": "number", "opt": true, "default": 10, "desc": "Top N files to report (default 10)" }
    } },
    { "tool": "unity_watch_add", "command": "watch_add", "path": "/watch/add", "description": "Add a runtime watch: sample a field/property every 0.5s during Play Mode and track its trend (↑/↓/=). Use to observe HP, ammo, state, or any runtime value over time. Only 'field' is required — component is auto-detected (the component holding that field, game scripts preferred), and objectName defaults to the selected GameObject. Supports nested paths like Damageable.Hp.Value.", "params": {
      "field": { "type": "string", "desc": "Field or property to watch, e.g. currentHp or Damageable.Hp.Value" },
      "objectName": { "type": "string", "opt": true, "desc": "GameObject name (fuzzy/Clone-aware); omit to use the selected object in Hierarchy" },
      "component": { "type": "string", "opt": true, "desc": "Component type name, e.g. PlayerController; omit to auto-detect which component has the field" }
    } },
    { "tool": "unity_watch_get", "command": "watch_get", "path": "/watch/get", "description": "Get the current sampled values and trend for all active runtime watches. Shows last 10 values per watch with timestamps.", "params": {} },
    { "tool": "unity_watch_clear", "command": "watch_clear", "path": "/watch/clear", "description": "Remove all runtime watches", "params": {} },
    { "tool": "unity_watch_alert", "command": "watch_alert", "path": "/watch/alert", "description": "Watch a field AND alert on a condition: each 0.5s sample, when the condition becomes true (rising edge) it logs a warning and increments a counter (surfaced in watch_get / the 👁 Watch panel). Great for catching glitches that flash by — value goes negative, exceeds a cap, or changes unexpectedly. Same auto-detection as watch_add (field-only is fine).", "params": {
      "field": { "type": "string", "desc": "Field/property to watch, e.g. currentHp" },
      "op": { "type": "string", "desc": "Condition: lt | lte | gt | gte | eq | ne (compare to value) or 'changed' (any change). Symbols < <= > >= == != also accepted." },
      "value": { "type": "number", "opt": true, "default": 0, "desc": "Threshold to compare against (ignored for 'changed')" },
      "objectName": { "type": "string", "opt": true, "desc": "GameObject name; omit to use the selected object" },
      "component": { "type": "string", "opt": true, "desc": "Component type; omit to auto-detect" }
    } },
    { "tool": "unity_watch_animator", "command": "watch_animator", "path": "/watch/animator", "description": "Watch an Animator live during Play: the current state (clip + normalized time, flags transitions) or a parameter value. Use to debug stuck/untriggered animations that ordinary field watches can't reach. Shows up in watch_get and the 👁 Watch panel.", "params": {
      "objectName": { "type": "string", "opt": true, "desc": "GameObject with the Animator; omit to use the selected object" },
      "param": { "type": "string", "opt": true, "desc": "Animator parameter name to watch (Float/Int/Bool/Trigger); omit to watch the current state instead" }
    } },
    { "tool": "unity_event_log", "command": "event_log", "path": "/event/log", "description": "Attach a temporary probe to a GameObject (selected if omitted) that logs its physics events live during Play — OnCollisionEnter/Exit and OnTriggerEnter/Exit. Use to debug 'why didn't it hit / hit twice / trigger never fired'. The object needs a Collider (+ Rigidbody for collisions; isTrigger for triggers). Probes auto-detach on Stop. Read results with unity_event_log_get.", "params": {
      "name": { "type": "string", "opt": true, "desc": "GameObject name to probe; omit to use the selected object" }
    } },
    { "tool": "unity_event_log_get", "command": "event_log_get", "path": "/event/log-get", "description": "Get the recent physics events captured by event_log probes (newest first): time, kind (collisionEnter/Exit, triggerEnter/Exit), self, other.", "params": {} },
    { "tool": "unity_event_log_clear", "command": "event_log_clear", "path": "/event/log-clear", "description": "Detach all event_log probes and clear the captured event buffer.", "params": {} },
    { "tool": "unity_read_scriptableobject", "command": "read_scriptableobject", "path": "/asset/so-read", "description": "Read the serialized values of a ScriptableObject asset (config / balance / game data). Use before edit_scriptableobject to see exact property names.", "params": {
      "path": { "type": "string", "opt": true, "desc": "Asset path, e.g. Assets/Data/HeroConfig.asset" },
      "name": { "type": "string", "opt": true, "desc": "Or the asset name to search for (t:ScriptableObject)" }
    } },
    { "tool": "unity_edit_scriptableobject", "command": "edit_scriptableobject", "path": "/asset/so-edit", "description": "Set a serialized property on a ScriptableObject asset (tune balance/config data). Primitive values only (number/bool/text/enum, 'x,y,z' Vector3, '#RRGGBB' Color) — same value format as set_property. Requires Write ON.", "params": {
      "path": { "type": "string", "opt": true, "desc": "Asset path, e.g. Assets/Data/HeroConfig.asset" },
      "name": { "type": "string", "opt": true, "desc": "Or the asset name to search for" },
      "property": { "type": "string", "desc": "Property name or display name, e.g. maxHp" },
      "value": { "type": "string", "desc": "Value as string" }
    } },
    { "tool": "unity_raycast", "command": "raycast", "path": "/scene/raycast", "description": "Cast a physics ray and report what it hits (object, layer, distance, point, normal) — debug combat/targeting ('why doesn't it hit / hits the wrong thing'). Give a direction (dx,dy,dz) or a target point (tx,ty,tz). Works in edit or play if colliders exist.", "params": {
      "ox": { "type": "number", "opt": true, "default": 0 }, "oy": { "type": "number", "opt": true, "default": 0 }, "oz": { "type": "number", "opt": true, "default": 0 },
      "dx": { "type": "number", "opt": true, "default": 0 }, "dy": { "type": "number", "opt": true, "default": 0 }, "dz": { "type": "number", "opt": true, "default": 0 },
      "tx": { "type": "number", "opt": true, "default": 0 }, "ty": { "type": "number", "opt": true, "default": 0 }, "tz": { "type": "number", "opt": true, "default": 0 },
      "maxDistance": { "type": "number", "opt": true, "default": 1000, "desc": "Max ray length" },
      "all": { "type": "boolean", "opt": true, "default": false, "desc": "Return all hits (sorted) instead of the first" },
      "layers": { "type": "string", "opt": true, "desc": "Comma-separated layer names to include; omit = all layers" }
    } },
    { "tool": "unity_overlap", "command": "overlap", "path": "/scene/overlap", "description": "Find colliders within a sphere (center + radius) — 'what's near this point' for AoE/aggro/pickup debugging. Returns object names, layers, and distance from center.", "params": {
      "cx": { "type": "number", "opt": true, "default": 0 }, "cy": { "type": "number", "opt": true, "default": 0 }, "cz": { "type": "number", "opt": true, "default": 0 },
      "radius": { "type": "number", "desc": "Sphere radius" },
      "layers": { "type": "string", "opt": true, "desc": "Comma-separated layer names to include; omit = all" }
    } },
    { "tool": "unity_navmesh_path", "command": "navmesh_path", "path": "/scene/navmesh-path", "description": "Compute a NavMesh path between two points — debug AI that can't reach / gets stuck. Returns status (PathComplete/Partial/Invalid), corner count, total distance, and the corner list. Partial/Invalid means unreachable (point off the NavMesh, a gap, or a blocked area).", "params": {
      "fx": { "type": "number", "opt": true, "default": 0 }, "fy": { "type": "number", "opt": true, "default": 0 }, "fz": { "type": "number", "opt": true, "default": 0 },
      "tx": { "type": "number", "opt": true, "default": 0 }, "ty": { "type": "number", "opt": true, "default": 0 }, "tz": { "type": "number", "opt": true, "default": 0 }
    } },
    { "tool": "unity_console_alert", "command": "console_alert", "path": "/console/alert", "description": "Watch the console for log messages matching a substring (case-insensitive) and count them + keep the last few — like watch_alert but for Debug.Log/Warning/Error. Catches errors that scroll away during play. Poll with console_alert_get.", "params": {
      "pattern": { "type": "string", "desc": "Substring to match in log messages, e.g. 'NullReference' or 'desync'" },
      "level": { "type": "string", "opt": true, "desc": "all | warning | error (minimum severity to match); default all" }
    } },
    { "tool": "unity_console_alert_get", "command": "console_alert_get", "path": "/console/alert-get", "description": "Get console_alert results: each pattern's match count and last matched messages.", "params": {} },
    { "tool": "unity_console_alert_clear", "command": "console_alert_clear", "path": "/console/alert-clear", "description": "Remove all console_alert patterns.", "params": {} },
    { "tool": "unity_exceptions", "command": "get_exceptions", "path": "/diagnose/exceptions", "description": "Get recent Unity exceptions and errors (rolling buffer of last 50, deduplicated). Sorted by lastSeen desc. Use to quickly surface runtime exceptions without opening the Console window.", "params": {} },
    { "tool": "unity_run_csharp", "command": "run_csharp", "path": "/code/run", "timeoutMs": 60000, "noRetry": true, "description": "Escape hatch — compile and run arbitrary C# against the live Editor/scene via Roslyn. Use ONLY when no dedicated tool fits: build prefabs from imported models, batch-edit assets, drive the AssetDatabase/importer, or prototype gameplay live. Provide a FULL C# source file in 'code'. Put logic in 'public static string Run()' (its return string is reported back), or define a MonoBehaviour (auto-attached to a new GameObject in the playing scene). All loaded assemblies (UnityEngine, UnityEditor, game asm) are referenced automatically. Requires Write ON. Make scripts idempotent — there is no retry, but rerunning should be safe.", "params": {
      "code": { "type": "string", "desc": "Full C# source. Logic in 'public static string Run()' (return value reported) or a MonoBehaviour (auto-attached live)." }
    } },
    { "tool": "unity_edit_script", "command": "edit_script", "path": "/script/edit", "noRetry": true, "description": "Apply a targeted find/replace edit to an EXISTING project .cs file — the way to fix bugs/refactor without rewriting the whole file. Read the file with unity_read_script first to copy the exact text. After editing, call unity_compile then poll unity_compile_status until ready. Requires Write ON.", "params": {
      "name": { "type": "string", "opt": true, "desc": "Script name (e.g. PlayerController) — resolved via the codebase index" },
      "path": { "type": "string", "opt": true, "desc": "Or the asset path directly, e.g. Assets/Scripts/PlayerController.cs (takes precedence over name)" },
      "find": { "type": "string", "desc": "Exact existing text to replace (include enough surrounding lines to be unique)" },
      "replace": { "type": "string", "opt": true, "desc": "Replacement text (empty to delete the matched text)" },
      "all": { "type": "boolean", "opt": true, "default": false, "desc": "Replace every occurrence; otherwise the match must be unique" }
    } },
    { "tool": "unity_assign_reference", "command": "assign_reference", "path": "/object/assign-reference", "description": "Assign an object/asset REFERENCE to a component field (what set_property can't do — it only handles primitives). Wires e.g. a GameObject/Component target or a prefab/material asset into a serialized object-reference field. Requires Write ON.", "params": {
      "name": { "type": "string", "desc": "GameObject name in the active scene that holds the component" },
      "component": { "type": "string", "opt": true, "desc": "Component type name; empty = Transform" },
      "property": { "type": "string", "desc": "Object-reference field name on the component" },
      "target": { "type": "string", "desc": "Scene GameObject name, OR an asset name/path. The right component on the target is picked to match the field type." },
      "asset": { "type": "boolean", "opt": true, "default": false, "desc": "Force resolving 'target' as a project asset (not a scene object)" }
    } },
    { "tool": "unity_run_batch", "command": "run_batch", "path": "/batch", "timeoutMs": 30000, "noRetry": true, "description": "Run multiple Unity commands in one call (build a whole scene/setup in a single round-trip). Each item is a normal command object and goes through the same write-gate/rate-limit. Max 50. Returns per-item ok/result. Requires Write ON.", "params": {
      "commands": { "type": "object[]", "desc": "Array of command objects, e.g. [{\"command\":\"create_gameobject\",\"name\":\"A\",\"primitive\":\"cube\"},{\"command\":\"set_transform\",\"name\":\"A\",\"set\":\"pos\",\"px\":2}]" }
    } },
    { "tool": "unity_delete_asset", "command": "delete_asset", "path": "/asset/delete", "noRetry": true, "description": "Delete a project asset FILE (moves it to the OS trash — recoverable). Refuses folders, third-party/plugin paths, and anything outside Assets/. Requires Write ON.", "params": {
      "path": { "type": "string", "desc": "Asset path under Assets/, e.g. Assets/Prefabs/Old.prefab" }
    } },
    { "tool": "unity_set_import_settings", "command": "set_import_settings", "path": "/asset/import-settings", "noRetry": true, "description": "Change a texture's import settings (the fix that pairs with unity_audit_textures — apply its recommendations). Only the fields you pass are changed, then the asset is reimported. Requires Write ON.", "params": {
      "path": { "type": "string", "desc": "Texture asset path, e.g. Assets/Art/icon.png" },
      "maxSize": { "type": "number", "opt": true, "desc": "Max texture size (e.g. 512, 1024, 2048); 0 = leave unchanged" },
      "compression": { "type": "string", "opt": true, "desc": "none | low | normal | high" },
      "readable": { "type": "string", "opt": true, "desc": "'true'/'false' — Read/Write Enabled (false saves 2x memory)" },
      "mipmaps": { "type": "string", "opt": true, "desc": "'true'/'false' — Generate Mip Maps" },
      "crunch": { "type": "string", "opt": true, "desc": "'true'/'false' — Crunch compression (smaller on disk)" }
    } },
    { "tool": "unity_capture_screenshot", "command": "capture_screenshot", "path": "/view/screenshot", "description": "Capture the Game or Scene view to a PNG and return it as an actual image (the bridge reads the file off disk and attaches it) — use to SEE the result after a change (great for Art/UI). By default renders the camera (no Screen-Space-Overlay UI); set overlay=true during Play to grab the real backbuffer including overlay UI (at screen resolution). Read-only.", "params": {
      "view": { "type": "enum", "values": ["game","scene"], "opt": true, "default": "game", "desc": "Which camera to capture" },
      "width": { "type": "number", "opt": true, "default": 1280, "desc": "Output width (64..4096); ignored when overlay=true" },
      "height": { "type": "number", "opt": true, "default": 720, "desc": "Output height (64..4096); ignored when overlay=true" },
      "overlay": { "type": "boolean", "opt": true, "default": false, "desc": "Play-only: capture the real backbuffer incl. Screen-Space-Overlay UI (at screen res)" },
      "path": { "type": "string", "opt": true, "desc": "Custom output path (Assets/... or absolute); default Library/AIUnityMCPServer/screenshots/" },
      "base64": { "type": "boolean", "opt": true, "default": false, "desc": "Also embed the PNG as base64 in the JSON (capped ~3MB); the bridge already returns the image regardless" }
    } },
    { "tool": "unity_build_player", "command": "build_player", "path": "/build/player", "timeoutMs": 600000, "noRetry": true, "description": "Build a standalone/mobile player via BuildPipeline. Blocking and can take minutes (the bridge may time out while the build continues). Uses enabled Build Settings scenes unless 'scenes' is given. Switches the active build target if needed. Requires Write ON.", "params": {
      "path": { "type": "string", "desc": "Output path, e.g. Builds/win/Game.exe (or a folder for some targets)" },
      "target": { "type": "string", "opt": true, "desc": "win64 | win32 | android | ios | webgl | mac | linux64; empty = current active target" },
      "scenes": { "type": "string", "opt": true, "desc": "Override scene list, paths separated by ';' (default: enabled Build Settings scenes)" },
      "dev": { "type": "boolean", "opt": true, "default": false, "desc": "Development build (debug symbols, profiler)" }
    } },
    { "tool": "unity_git_status", "command": "git_status", "path": "/git/status", "description": "Show the project's git branch and working-tree changes (porcelain) — use to see what was modified before suggesting a commit. Read-only.", "params": {} },
    { "tool": "unity_run_tests", "command": "run_tests", "path": "/tests/run", "noRetry": true, "description": "Start a Unity Test Runner run and return immediately (results are async — poll unity_get_test_results until status='done'). EditMode by default; PlayMode enters play. Requires the com.unity.test-framework package. Requires Write ON.", "params": {
      "mode": { "type": "enum", "values": ["edit","play"], "opt": true, "default": "edit", "desc": "EditMode (fast, no play) or PlayMode (enters play, slower)" },
      "filter": { "type": "string", "opt": true, "desc": "Optional test-name regex (groupNames) to run a subset, e.g. 'MyNamespace.Combat.*'" }
    } },
    { "tool": "unity_get_test_results", "command": "get_test_results", "path": "/tests/results", "description": "Poll the latest Unity Test Runner results: status (idle/running/done), pass/fail/skip/total counts, and the list of failed tests with messages. Call repeatedly after unity_run_tests until status='done'. Read-only.", "params": {} }
  ]
}
